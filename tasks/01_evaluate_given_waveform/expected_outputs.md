# Expected Outputs — Task 01

## Overview

Running `solve_task.py` with a valid input waveform should produce:

```text
outputs/
  motion_trace.png
  residual_time_series.png
  excitation_summary.txt
